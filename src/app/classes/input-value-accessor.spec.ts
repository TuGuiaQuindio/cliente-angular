import { InputValueAccessor } from './input-value-accessor';

describe('InputValueAccessor', () => {
  it('should create an instance', () => {
    expect(new InputValueAccessor()).toBeTruthy();
  });
});
