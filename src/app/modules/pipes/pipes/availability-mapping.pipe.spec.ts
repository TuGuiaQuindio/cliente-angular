import { AvailabilityMappingPipe } from './availability-mapping.pipe';

describe('AvailabilityMappingPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailabilityMappingPipe();
    expect(pipe).toBeTruthy();
  });
});
