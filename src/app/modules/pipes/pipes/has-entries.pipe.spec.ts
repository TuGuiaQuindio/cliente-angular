import { HasEntriesPipe } from './has-entries.pipe';

describe('HasEntriesPipe', () => {
  it('create an instance', () => {
    const pipe = new HasEntriesPipe();
    expect(pipe).toBeTruthy();
  });
});
